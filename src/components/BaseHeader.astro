<header class="header">
  <div class="container">
    <div class="header__container">
      <a class="header__logo" href="/" aria-label="Link to Home page">
        <img src="/assets/images/redradix-logo.svg" alt="Redradix Logo" />
      </a>
      <div class="header__menu-button">
        <img
          class="header__menu-icon"
          src="/assets/icons/menu-icon.svg"
          alt="Menu button"
        />
      </div>
    </div>
  </div>
</header>

<style lang="scss">
  @import '../styles/scss/abstracts/_variables';
  @import '../styles/scss/abstracts/_mixins';

  /* Local variables */
  $logo-width-m: 22px;
  $logo-width-d: 36px;

  .header {
    width: 100%;

    @include media(md) {
      min-height: $header-height-d;
    }

    &__container {
      display: flex;
      align-items: center;
      justify-content: space-between;
      align-items: stretch;

      padding: $space-s 0;

      @include media(md) {
        padding: $space-xl2 0;
      }
    }

    &__logo {
      max-width: $logo-width-m;

      @include media(md) {
        max-width: $logo-width-d;
      }
    }

    &__menu-button {
      display: flex;
      align-items: center;
      justify-content: space-between;
      cursor: pointer;
    }

    &__menu-icon {
      max-width: $space-l;
    }
  }
</style>
